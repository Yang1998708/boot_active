server:
  port: 8888
  
spring:
  activemq:
    broker-url: tcp://192.168.227.130:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false   #false = Queue true=topic 不写默认队列
    
#自己定义队列名称
myqueue: boot-activemq-queue